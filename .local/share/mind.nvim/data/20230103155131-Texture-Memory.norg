* Texture Memory

  - Rescale units, s.t. grid fits in 1x1 square
    -- Rescale mu_a, mu_s
    -- Check math for other scaling behaviour
  - Clip position to [0,0,0], [1,1,1]
  - Calculate grid index for position (un-scale and parse to integer)

